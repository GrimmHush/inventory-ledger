PORT=3000
API_KEY=dev-key

# Optional. When set, the server uses the Postgres-backed store instead of the
# in-memory one. This value matches the local database in docker-compose.yml.
DATABASE_URL=postgresql://inventory_user:inventory_pass@localhost:5432/inventory_db?schema=public
